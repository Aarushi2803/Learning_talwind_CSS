const navDialog = document.getElementById('nav-dialog');
function handleMenu() {
    navDialog.classList.toggle('hidden');
}

function setupsectionObserver(element,isLTF,speed){
    const intersectionObserver = new IntersectionObserver();
}